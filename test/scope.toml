[fetch.lib]
name = "nixpkgs.lib"
import = true
sub = "lib"
optional = true

[features]
lib = []
default = []

[backends.nix]
root = "std"
fetcher = "npins"

[project.meta]
# special attribute
# for the Nix std library
__is_std__ = true
